package pl.jakub.ultraopt.config;

public class UltraOptConfig {

    // główny przełącznik moda
    public boolean enabled = true;

    // itemy
    public boolean itemPhysics = true;
    public boolean itemCounter = true;
    public boolean itemSingleModel = true;

    // particle (na przyszłość)
    public boolean particles = true;
}
